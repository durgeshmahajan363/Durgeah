const express = require("express");
const app = express();

const { selectUser } = obj;
/*app.get("/user" (req,res) => {
    res.json(obj);
});*/

app.listen(4000, console.log("server started"));
